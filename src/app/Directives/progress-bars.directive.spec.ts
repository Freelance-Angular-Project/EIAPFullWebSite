import { ProgressBarsDirective } from './progress-bars.directive';

describe('ProgressBarsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProgressBarsDirective();
    expect(directive).toBeTruthy();
  });
});
